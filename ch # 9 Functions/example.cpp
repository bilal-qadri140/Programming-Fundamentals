#include <iostream>
using namespace std;
int sum(int, int);
int main()
{
    int a = 20, b = 25;
    sum(a, b);
    cout<<"gf"<<sum;
}
int sum(int x, int y)
{
    
    return (x + y);
}
